// Placeholder test — the actual app uses Firebase which requires real credentials.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder', (WidgetTester tester) async {
    // Integration tests are run separately with Firebase test credentials.
    expect(true, isTrue);
  });
}
