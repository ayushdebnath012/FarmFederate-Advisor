# FarmFederate Deployment Requirements (Optimized for Render Free Tier)
# Uses CPU-only PyTorch to reduce memory footprint

# Core ML - CPU only (smaller download, less memory)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# Transformers (for text encoding)
transformers>=4.30.0
tokenizers>=0.13.0

# Data Processing
numpy>=1.23.0
Pillow>=9.5.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Progress
tqdm>=4.65.0
